# PostgreSQL - Docker (local-postgres), admin / password
# blog_db olusturmak: docker exec -it local-postgres psql -U admin -d panorama3d_db -c "CREATE DATABASE blog_db;"
DATABASE_URL="postgresql://admin:password@localhost:5432/blog_db"

# API portu (opsiyonel, varsayılan 3000)
PORT=3000
